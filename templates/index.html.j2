<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <!-- Primary Meta Tags -->
        <title>{{ name }} - {{ title }}</title>
        <meta name="title" content="{{ name }} - {{ title }}">
        <meta name="description" content="{{ description }}">
        <!-- Open Graph / Facebook -->
        <meta property="og:type" content="website">
        <meta property="og:url" content="{{ site_url }}">
        <meta property="og:title" content="{{ name }} - {{ title }}">
        <meta property="og:description" content="{{ description }}">
        <meta property="og:image" content="{{ avatar }}">
        <!-- Twitter -->
        <meta property="twitter:card" content="summary_large_image">
        <meta property="twitter:url" content="{{ site_url }}">
        <meta property="twitter:title" content="{{ name }} - {{ title }}">
        <meta property="twitter:description" content="{{ description }}">
        <meta property="twitter:image" content="{{ avatar }}">
        <meta property="twitter:creator" content="{{ twitter_handle }}">
        <!-- Favicon -->
        <link rel="icon" type="image/png" href="{{ avatar }}">
        <!-- Font Awesome -->
        <link rel="stylesheet"
              href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.1/css/all.min.css">
        <!-- Verification -->
        <link rel="me" href="https://hachyderm.io/@miketheman">
        <style>
    :root {
      --bg-color: #fafafa;
      --text-color: #1a1a1a;
      --link-bg: #f0f0f0;
      --link-hover: #e0e0e0;
      --secondary-text-color: #666;
    }

    @media (prefers-color-scheme: dark) {
      :root {
        --bg-color: #1a1a1a;
        --text-color: #fafafa;
        --link-bg: #2a2a2a;
        --link-hover: #3a3a3a;
        --secondary-text-color: #999;
      }
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: var(--bg-color);
      color: var(--text-color);
      line-height: 1.6;
      margin: 0;
      padding: 20px;
      display: flex;
      justify-content: center;
      min-height: 100vh;
    }

    .container {
      max-width: 600px;
      width: 100%;
      padding: 20px;
    }

    .profile {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 20px;
    }

    .avatar {
      width: 100%;
      max-width: 512px;
      height: auto;
      overflow: hidden;
    }

    .avatar img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
    }

    @media (max-width: 600px) {
      .avatar {
        max-width: 300px;
      }
    }

    .name {
      font-size: 24px;
      font-weight: bold;
      margin: 0;
    }

    .title {
      font-size: 18px;
      color: var(--secondary-text-color);
      margin: 0;
    }

    .description {
      text-align: center;
      max-width: 500px;
      margin: 0 auto;
    }

    .links {
      display: flex;
      flex-direction: column;
      gap: 12px;
      width: 100%;
      margin-top: 20px;
    }

    .link-item {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 16px;
      background: var(--link-bg);
      border-radius: 8px;
      text-decoration: none;
      color: var(--text-color);
      transition: background-color 0.2s;
    }

    .link-item i {
      font-size: 24px;
    }

    .link-item:hover {
      background: var(--link-hover);
    }

    .extras-section {
      margin-top: 40px;
      width: 100%;
    }

    .extras-heading {
      font-size: 18px;
      font-weight: 600;
      margin-bottom: 12px;
      text-align: center;
      color: var(--text-color);
    }

    .extras-links {
      display: flex;
      flex-direction: column;
      gap: 12px;
      width: 100%;
    }

    .extra-link-content {
      flex: 1;
      display: flex;
      flex-direction: column;
      gap: 4px;
    }

    .extra-link-date {
      font-size: 12px;
      color: var(--secondary-text-color);
    }

    .footer {
      margin-top: 40px;
      text-align: center;
      font-size: 14px;
      color: var(--secondary-text-color);
    }
        </style>
    </head>
    <body>
        <div class="container">
            <div class="profile">
                <div class="avatar">
                    <img src="{{ avatar }}" alt="{{ name }}">
                </div>
                <h1 class="name">{{ name }}</h1>
                <p class="title">{{ title }}</p>
                <p class="description">{{ description }}</p>
                <div class="links">
                    {% for link in links %}
                        <a href="{{ link.url }}"
                           class="link-item"
                           rel="noopener noreferrer"
                           target="_blank">
                            <i class="{{ link.icon }}"></i>
                            <span>{{ link.label }}</span>
                        </a>
                    {% endfor %}
                </div>
                {% if extras %}
                <div class="extras-section">
                    <div class="extras-heading">Extras</div>
                    <div class="extras-links">
                        {% for extra in extras %}
                            <!-- Extra links use a wrapper div to support optional date display below label -->
                            <a href="{{ extra.url }}"
                               class="link-item"
                               rel="noopener noreferrer"
                               target="_blank">
                                <i class="{{ extra.icon }}"></i>
                                <div class="extra-link-content">
                                    <span>{{ extra.label }}</span>
                                    {% if extra.date %}
                                    <span class="extra-link-date">{{ extra.date }}</span>
                                    {% endif %}
                                </div>
                            </a>
                        {% endfor %}
                    </div>
                </div>
                {% endif %}
                <div class="footer">Updated {{ updated_date }}</div>
            </div>
        </div>
    </body>
</html>
